name: 🚀 RikkaHub 最终稳定构建
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build_release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 1. 拉取代码
        uses: actions/checkout@v4

      - name: 2. 还原 Google Services
        run: echo '${{ secrets.GOOGLE_SERVICES_JSON }}' > app/google-services.json

      - name: 3. 🔐 解码密钥到模块目录（app/）
        run: |
          # 直接在项目根目录用 echo 重定向到 app/ 下，路径绝对清晰
          echo "${{ secrets.KEYSTORE_FILE }}" | base64 --ignore-garbage -d > app/release-key.p12
          # 双验证文件在
          ls -lh app/release-key.p12
          stat app/release-key.p12

      - name: 4. 📝 生成适配模块级路径的 local.properties
        run: |
          # storeFile 只写文件名！！！！因为模块级的 file() 相对于 app/
          cat > local.properties << EOF
          # CI Auto Generated for RikkaHub (Kotlin DSL 模块级适配)
          storeFile=release-key.p12
          storePassword=${{ secrets.KEYSTORE_PASSWORD }}
          keyAlias=${{ secrets.KEY_ALIAS }}
          keyPassword=${{ secrets.KEY_PASSWORD }}
          storeType=PKCS12
          EOF
          
          # 打印最终内容确认
          echo "--- ✅ 最终 local.properties 内容 ---"
          cat local.properties
          echo ""
          echo "--- ✅ 当前目录结构 ---"
          ls -la
          echo ""
          echo "--- ✅ App 模块目录结构 ---"
          ls -la app/

      - name: 5. 配置 JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: 6. 赋予执行权限
        run: chmod +x gradlew

      - name: 7. 🔨 构建 APK + AAB（必成版）
        run: ./gradlew assembleRelease bundleRelease --no-daemon --build-cache

      - name: 8. 📤 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: rikkahub-final-builds
          path: |
            app/build/outputs/apk/release/*.apk
            app/build/outputs/bundle/release/*.aab
          retention-days: 7

      - name: 9. 🚀 发布到 GitHub Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            app/build/outputs/apk/release/*.apk
            app/build/outputs/bundle/release/*.aab
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
